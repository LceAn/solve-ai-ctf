# 工作台全面调研与优化提案（供审阅）

> 调研日期：2026-09-06 · 基线：v0.2.1（R1-R43 完成，单测 141 项 + E2E 44 项全绿）
> 调研方式：浏览器逐页实测（demo-env 真实数据 + Docker 运行态）+ 前端/后端代码走读 + 43 轮迭代积累的问题清单
> **本文档只做调研与提案，未经审阅不动手。** 每项含：现状证据 → 问题 → 提案 → 优先级（P1 高 / P2 中 / P3 低）→ 预估工作量（S/M/L）。
> 你审阅时在「审阅意见」列打勾或批注即可，我按你批的执行。

---

## 总览：问题索引（按优先级）

| ID | 页面/范围 | 一句话 | 优先级 | 工作量 | 审阅意见 |
|---|---|---|---|---|---|
| A1 | AI 看板 | 泳道无自动刷新，需手动点 | P1 | S | |
| T1 | 运行任务 | 任务输出面板无搜索/自动滚动开关/下载 | P1 | M | |
| E4 | 环境 | 逐题构建无法批量勾选（只能逐个或全部） | P1 | S | |
| O2 | 比赛管理 | 三个开赛代理按钮无运行状态徽标，要切页才能看 | P1 | S | |
| E1 | 环境 | 每次进页加载 4-6s（WSL 串行 docker 调用），无缓存 | P1 | M | |
| B2 | 总览 | 题目卡片无"谁在打这题"（多 AI 协作时核心信息缺失） | P2 | M | |
| T3 | 运行任务 | 任务无类型过滤（求解/env 构建/推送混排） | P2 | S | |
| A2 | AI 看板 | 泳道任务不可点击跳转到运行任务页 | P2 | S | |
| TL1 | 时间线 | 事件流无按类型过滤 | P2 | S | |
| FI1 | 文件/日志 | 无文件名/内容搜索 | P2 | M | |
| FI2 | 文件/日志 | 附件无下载按钮（只读预览有了，下载没有） | P2 | S | |
| F1 | Flag 审核 | 无批量放行（逐个点，一血场景慢） | P2 | S | |
| K2 | 知识库 | 零命中查询清单只在 CLI --stats，页面不可见 | P2 | S | |
| E3 | 环境 | 推送无历史记录（推没推成功无处可查） | P2 | M | |
| E5 | 环境 | compose 题目服务无"直接 down"按钮 | P2 | S | |
| E2 | 环境 | L0 底座无重建/更新入口（只有 L1 有重建） | P2 | S | |
| O1 | 比赛管理 | 「注册题目」与「自动抓题」双入口新手易混 | P2 | S | |
| G4 | 全局 | 未捕获的 Promise 异常静默吞掉，无全局错误提示 | P2 | S | |
| G3 | 顶栏 | 下拉框已初始化/未初始化比赛混排无分组 | P3 | S | |
| B1 | 总览 | 统计条无"最近活动时间"维度 | P3 | S | |
| B3 | 总览 | 搜索不搜题面描述 | P3 | S | |
| D1 | 题目工作区 | 子页签不记忆上一次停留 | P3 | S | |
| D2 | 题目工作区 | 尝试记录表单长，缺"快速记录"模板 | P3 | M | |
| D3 | 题目工作区 | 假设优先级公式（P×分值÷耗时）不可见 | P3 | S | |
| A3 | AI 看板 | 无"仅失败/仅运行中"过滤 | P3 | S | |
| F2 | Flag 审核 | 自动提交配置入口在运维页，路径深 | P3 | S | |
| H1 | 系统概况 | 健康数据 30s 缓存但无自动刷新开关 | P3 | S | |
| H2 | 系统概况 | self_test 输出只显示最后一行 | P3 | S | |
| TL2 | 时间线 | 事件流无导出 | P3 | S | |
| DOC1 | 文档/WP | 比赛级 docs/ 无新建 md 入口 | P3 | S | |
| G5 | 全局 | 窄屏/移动端适配未验证 | P3 | M | |
| S3 | 全局 | 列表无虚拟化，100+ 题时有卡顿风险 | P3 | M | |
| S4 | 全局 | 任务详情无独立深链接 URL | P3 | S | |
| N-11尾 | app.js | ES module 全量拆分（R26 只抽了 core.js） | P2 | L | |

---

## 一、逐页调研

### 1. 顶栏与全局导航

**现状实测**：比赛下拉框含「＋ 新建比赛…」（R43）；已初始化/未初始化比赛混排；侧边栏 12 个页签 + 可折叠；`?` 呼出快捷键帮助；Alt+1..9 切页。

- **G3 下拉无分组**：demo-env（configured）与"未初始化"目录平铺。→ 提案：`<optgroup>` 分「已初始化 / 未初始化」两组，未初始化的灰显。S
- **G4 全局错误吞没**：`boot().catch` 只兜启动；页面内所有 `api().catch(() => {})` 静默。→ 提案：`window.addEventListener("unhandledrejection", ...)` 统一 toast（可去重限频），并在系统概况加"最近错误"卡片。S
- **G5 窄屏**：`tasks-layout`/`files-layout` 双栏在 <1000px 未做响应式。→ 提案：媒体查询单栏堆叠。M

### 2. 比赛总览（board）

**现状实测**：统计条（题目/已解/进行中/得分/artifacts/docs）+ 搜索框 + 状态筛选 + 类别分组卡片。数据来自 `/api/competition`。

- **B2 无"谁在打"**：多 AI 协作是既定方向，但总览卡片看不到某题当前是否有任务在跑/哪个 agent。任务数据在 `/api/tasks` 里现成。→ 提案：卡片右上角加运行中任务徽标（agent 名），数据合并 `/api/tasks` 按 slug 聚合。M
- **B1 统计静态**：→ 统计条尾部追加"最近事件 12 分钟前"（events.jsonl 末条时间）。S
- **B3 搜索不含描述**：→ 前端把 `description` 加入匹配字段即可（数据已在 competition_view）。S

### 3. 题目工作区（detail）

**现状实测**：方向子导航（含 R43 折叠）→ 题目选择 → 子页签（假设/尝试/证据/候选/日志等）→ 假设阶梯/有界执行表单。表单快照防打断机制已有。

- **D1 子页签不记忆**：切走再回来回到默认页签。→ localStorage 记忆最后子页签。S
- **D2 快速尝试**：尝试表单字段多（假设关联/结局/耗时/工件），抢节奏时嫌长。→ 预设"快速记录"：一行输入 + 默认 outcome=partial。M
- **D3 优先级公式不可见**：假设卡片不显示 priority 数值来源。→ 卡片角标显示 `P0.3×200÷60min`。S

### 4. AI 看板（board2）

**现状实测**：`/api/board` 返回题目泳道（case 事件）+ 任务泳道（时间跨度），时间窗可调，图例+手动刷新。

- **A1 无自动刷新**：作为"场上发生了什么"的仪表盘，却要手动点。→ 提案：开关"自动刷新（30s）"+ 收到 SSE `challenges_new`/任务完成事件时立即刷新。S
- **A2 泳道不可点**：任务泳道点一下应跳「运行任务」页并选中该任务（`TaskUI.selected` + setTab）。S
- **A3 无过滤**：→ 顶部加状态 chips（全部/运行中/失败）。S

### 5. Flag 审核（flags）

**现状实测**：五段流水线（扫描发现→待校验→可提交→已提交→已接受），逐条操作，一键复制已有。

- **F1 无批量放行**：→ 候选行加勾选框 + 「放行所选」批量 validate（仍走 case_manager 校验，安全模型不变）。S
- **F2 配置入口深**：→ 在本页顶部加 autosubmit 开关快捷入口（读写同一配置）。S

### 6. 运行任务（tasks）

**现状实测**：派发表单（题目/执行者/沙箱/网关/只看运行中）、任务列表（状态徽标/重派/停止）、输出 tail 面板。R8/R36/R41 已加强。

- **T1 输出面板**：无搜索、无自动滚动开关（现在似乎强制滚底）、无下载。→ 工具条：搜索高亮 / 暂停滚动 / 下载 .log。M
- **T3 类型混排**：求解任务与 env-build/env-push/猎手混在一列。→ agent 标签已可辨认，但缺类型过滤 chips（全部/求解/env/猎手）。S
- 任务类型识别：`task.agent` 字段已区分（solver/sandbox/env-builder/flag-agent）✓ 数据现成。

### 7. 🐳 环境（env）——本轮已二级页签化

**现状实测**：三页签（当前比赛/标准环境/仓库推送）+ Docker 运行态。实测发现：

- **E1 加载慢**：每次进页 `/api/env/status` 触发 `docker images`（WSL 一次 ~1-2s）+ `docker ps` + `docker system df` + 每镜像 inspect——合计 4-6s。→ 提案：服务端 15s 整体缓存（`_HEALTH_CACHE` 同款）+ 前端先渲染上次数据再原地刷新（stale-while-revalidate）。M
- **E4 批量构建**：逐题表格的「构建」按钮一次一个。→ 行首勾选框 + 「构建所选（并行）」（R34 的 --jobs 已就绪）。S
- **E3 推送无记录**：push 结果只在任务日志里。→ 每次 push 追加 `env/push-history.jsonl`，仓库推送页显示最近 10 条（时间/远端 tag/结果）。M
- **E5 服务 down**：compose 题目服务目前随任务结束自动 down，但想手动提前关要去命令行。→ 运行态容器行加「停止」按钮（按 compose project down）。S
- **E2 L0 无入口**：标准环境页 L0 只有徽标。→ L0 行加「重建」按钮（base 层重建 = sync-solver + docker build base，L1 提示跟随重建）。S

### 8. 系统概况（health）

- H1 自动刷新开关。S　H2 self_test 输出全文折叠展示。S

### 9. 时间线（timeline）

**现状实测**：SSE 实时推送 events.jsonl（3 条演示数据）。

- TL1 类型过滤 chips（challenge_registered/challenges_new/attempt/…）。S
- TL2 导出为 Markdown 附到复盘报告（competition.report 已能引用 events，缺独立导出）。S

### 10. 文件/日志（files）

- FI1 搜索：文件树按名过滤（前端即可）+ 当前打开文件内查找（浏览器 Ctrl+F 已可用，体验一般）。M
- FI2 下载：`/api/file` 已有只读读取，加 `&download=1`（Content-Disposition）+ 行尾下载按钮。S

### 11. 知识库（kb）

- K2 零命中可见：页面加「检索统计」小卡（读 `--stats` 输出，新增 `/api/kb/stats`）。S
- K1 命中高亮：结果行对命中词 `<mark>`。S

### 12. 文档 / WP（docs）

**现状实测**：比赛级 docs/*.md 列表（空）+ artifacts + 当前题目 WP。R43 已加本地文档路径浏览。

- DOC1 比赛级 md 新建：无入口。→ 「＋ 新建 writeup」按钮（文件名输入 → 模板内容 → 保存走白名单动作写 docs/）。S
- 注意：写 docs/ 需要新写动作 `docs.write`（服务端校验路径必须在 comp/docs/ 内，防逃逸）。

### 13. 比赛管理（ops）

**现状实测**：三个子页签（开赛自动化/注册题目/运维操作）+ 记忆上次子页签。

- O2 代理无状态徽标：→ 三个代理卡片右上角显示对应 agent 最近任务状态（数据 `/api/tasks` 现成）。S
- O1 双入口说明：注册题目（单个/手工）与自动抓题（批量）本就不同用途，但无文案区分。→ 卡片副标题写清适用场景。S
- 抓题代理卡片文案已更新（附件自动下载）✓。

### 14. 已知但暂不动的（记录在案）

- **N-11 尾巴**：app.js 1836 行，R26 只抽了 core.js。ES module 全量拆分收益是长期可维护性，但有回归风险（全局作用域语义变化），建议单独立项做（配合浏览器逐页回归）。
- **S3 虚拟化**：当前 2 题 demo 无感；真实比赛 100+ 题时看板/列表需评估。
- **N-10**：历史 13G 比赛目录仍未定位到（需要你给路径）。

---

## 二、系统性观察（不是单页问题）

1. **WSL 通道的延迟预算**：所有 docker 调用经 `wsl docker`，单次 0.5-2s。凡是"点一下等一下"的功能（env/board/health）都受此放大。→ 系统性对策：统一加服务端短 TTL 缓存（R34 的 images 缓存模式推广）+ 前端 stale-while-revalidate。
2. **错误呈现不一致**：有的地方 toast、有的内嵌红字、有的静默。→ 统一错误处理规范（G4 一并做）。
3. **测量时序教训**：本次调研中 docs 页签曾误报"渲染成知识库"——复核后是走查脚本 500ms 等待不足的时序误差。已确认非 bug。后续审计以"两次复核一致"为准。
4. **安全模型没有因为迭代放松**：cap-drop/断网/提交白名单/argv 化/路径逃逸防护在 43 轮里保持一致——这是刻意设计，提案里所有新交互都遵守（例如 docs 写动作也要白名单+路径校验）。

---

## 三、建议执行批次（审阅后按批开工）

| 批次 | 内容 | 轮次 |
|---|---|---|
| **A 体验速赢** | A1 看板自动刷新 · O2 代理徽标 · E4 批量构建 · E2 L0 重建 · E5 服务 down · T3 类型过滤 · K2 零命中 UI | R44-R47（每轮 1-2 项） |
| **B 协作深化** | B2 题目卡片"谁在打" · A2 泳道跳转 · T1 输出工具条 · F1 批量放行 · FI2 下载 | R48-R51 |
| **C 性能与稳定** | E1/G1 服务端缓存 + stale-while-revalidate · G4 全局错误 · G3 下拉分组 | R52-R54 |
| **D 大件** | app.js ES module 全量拆分（N-11 收尾）· 窄屏适配 · S3 虚拟化评估 | 单独安排 |

预算外的"内容型"事项（不属于 UI 迭代，需要你参与）：N-09 语料回填（每场比赛赛后按模板）、N-10 历史目录定位与治理。

---

## 四、审阅结论区（由你填写）

- 批准批次：A / B / C / D（圈选或写明）
- 调整项（ID + 意见）：
- 追加需求：
- 明确不做：
